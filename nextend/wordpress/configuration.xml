<root>
    <params name="cache" label="Cache">
        <param type="clearcache" name="clearcache" label="Clear cache" />
        <param type="onoff" name="gzip" label="Gzip compression" description="Only enable it, when the mod_deflate not enabled in apache!" />
        <param type="text" name="cachetime" label="Cache time" default="900" description="1 or higher" style="width: 60px;">
            <unit>sec</unit>
        </param>
        <param type="onoff" name="safemode" default="0" label="Load CSS and Javascript in safe mode" description="Safe mode solves a problem when the stylesheet is missing, but it could cause flickering." />
    </params>
</root>